const API_BASE_URL = __ENV.API_BASE_URL;
const IR_TASK_UUID = __ENV.IR_TASK_UUID;

const BATCH_SIZE = __ENV.BATCH_SIZE;
const NUM_REQUESTS_PER_SECOND = __ENV.NUM_REQUESTS_PER_SECOND;
const BENCHMARK_TIMING_IN_SECONDS = __ENV.BENCHMARK_TIMING_IN_SECONDS;

const HEADERS = {
  "X-Api-Key": __ENV.API_KEY,
  "Content-Type": "application/json",
};


export { API_BASE_URL, HEADERS, IR_TASK_UUID, BATCH_SIZE, NUM_REQUESTS_PER_SECOND, BENCHMARK_TIMING_IN_SECONDS };
